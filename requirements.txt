# torch / torchaudio are installed separately by installer/torch_installer.py
# with the CUDA build matching the detected driver — not listed here.

fastapi>=0.111
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9
huggingface-hub>=0.23
soundfile>=0.12
